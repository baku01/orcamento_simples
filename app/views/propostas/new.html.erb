<!DOCTYPE html>
<html>
<head>
  <title>Nova Proposta</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%# Os helpers abaixo são específicos do Rails, mantenha-os se estiver no ambiente Rails %>
  <%# <%= csrf_meta_tags %>
  <%# <%= csp_meta_tag %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<style>
  /* Importação de fonte moderna */
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

  /* Paleta de Cores e Variáveis Globais */
  :root {
    --primary-color: #6366f1;
    --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    --secondary-gradient: linear-gradient(135deg, #06b6d4 0%, #3b82f6 100%);
    --success-color: #10b981;
    --success-gradient: linear-gradient(135deg, #10b981 0%, #34d399 100%);
    --danger-bg: #fef2f2;
    --danger-text: #dc2626;
    --danger-border: #fca5a5;
    --body-bg: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    --card-bg: rgba(255, 255, 255, 0.95);
    --text-color: #1e293b;
    --text-muted: #64748b;
    --border-color: #e2e8f0;
    --input-bg: #ffffff;
    --input-focus: #f8fafc;
    --shadow-soft: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    --shadow-hover: 0 20px 40px -10px rgba(0, 0, 0, 0.15), 0 8px 15px -5px rgba(0, 0, 0, 0.1);
    --shadow-input: 0 4px 8px -2px rgba(0, 0, 0, 0.05);
    --shadow-input-focus: 0 8px 20px -5px rgba(99, 102, 241, 0.3);
    --border-radius: 16px;
    --border-radius-sm: 12px;
    --border-radius-input: 10px;
    --transition-fast: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    --font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  }

  body {
    background: var(--body-bg);
    font-family: var(--font-family);
    min-height: 100vh;
    padding: 2rem 0;
    color: var(--text-color);
    font-size: 14px;
    line-height: 1.6;
  }

  .main-container {
    max-width: 950px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Card Principal com glassmorphism melhorado */
  .form-card {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.4);
    border-radius: var(--border-radius);
    box-shadow: var(--shadow-soft);
    overflow: hidden;
    transition: var(--transition-fast);
    position: relative;
  }

  .form-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--primary-gradient);
    z-index: 1;
  }

  .form-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-hover);
  }

  .card-header {
    background: var(--primary-gradient);
    color: white;
    padding: 2rem 2.5rem;
    border-bottom: none;
    position: relative;
    overflow: hidden;
  }

  .card-header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -10%;
    width: 200px;
    height: 200px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    transform: rotate(45deg);
  }

  .card-header h2 {
    font-weight: 600;
    font-size: 1.75rem;
    margin: 0;
    display: flex;
    align-items: center;
    gap: 1rem;
    position: relative;
    z-index: 2;
  }

  .card-header h2 i {
    font-size: 2rem;
    opacity: 0.9;
  }

  .card-body {
    padding: 2.5rem;
  }

  /* Labels melhorados */
  .form-label {
    font-weight: 600;
    color: var(--text-color);
    margin-bottom: 0.75rem;
    font-size: 0.875rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .form-label i {
    color: var(--primary-color);
    font-size: 1rem;
  }

  /* Input Groups completamente redesenhados */
  .input-group {
    position: relative;
    margin-bottom: 0.5rem; /* Removido margin-bottom maior para evitar espaçamento excessivo */
    transition: var(--transition-fast);
  }

  .input-group:focus-within {
    transform: translateY(-2px);
  }

  .input-group-text {
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border: 2px solid var(--border-color);
    border-right: none;
    color: var(--primary-color);
    font-weight: 600;
    font-size: 0.9rem;
    border-radius: var(--border-radius-input) 0 0 var(--border-radius-input);
    padding: 0 1rem;
    display: flex;
    align-items: center;
    min-width: 60px; /* Ajustado para consistência */
    justify-content: center;
    transition: var(--transition-fast);
  }

  .input-group:focus-within .input-group-text {
    border-color: var(--primary-color);
    background: linear-gradient(135deg, #eef2ff 0%, #e0e7ff 100%);
    color: var(--primary-color);
    box-shadow: var(--shadow-input-focus);
  }

  /* Form Controls modernos */
  .form-control {
    height: 56px;
    border: 2px solid var(--border-color);
    /* A regra 'border-left: none' foi removida porque o Bootstrap cuida disso no input-group */
    border-radius: 0 var(--border-radius-input) var(--border-radius-input) 0;
    font-size: 1rem;
    font-weight: 500;
    padding: 0 1.25rem;
    background: var(--input-bg);
    color: var(--text-color);
    transition: var(--transition-fast);
    box-shadow: var(--shadow-input);
  }

  /* Adicionando uma regra para o Bootstrap 5 garantir que o input se estique corretamente */
  .input-group > .form-control {
    flex: 1 1 auto;
    width: 1%;
    min-width: 0;
  }


  .form-control::placeholder {
    color: var(--text-muted);
    font-weight: 400;
    opacity: 0.7;
  }

  .form-control:focus {
    border-color: var(--primary-color);
    background: var(--input-focus);
    box-shadow: var(--shadow-input-focus);
    outline: none;
    transform: none;
  }

  .form-control:hover:not(:focus) {
    border-color: #cbd5e1;
    box-shadow: 0 6px 15px -3px rgba(0, 0, 0, 0.08);
  }

  /* Estados de validação melhorados */
  .form-control.is-valid {
    border-color: var(--success-color);
    background: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 100%);
    box-shadow: 0 4px 15px -3px rgba(16, 185, 129, 0.2);
  }

  .form-control.is-valid + .input-group-text {
    border-color: var(--success-color);
    background: linear-gradient(135deg, #f0fdf4 0%, #ecfdf5 100%);
    color: var(--success-color);
  }

  .form-control.is-invalid {
    border-color: var(--danger-text);
    background: var(--danger-bg);
    box-shadow: 0 4px 15px -3px rgba(220, 38, 38, 0.2);
  }

  /* Feedback visual para inputs */
  .input-group::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--primary-gradient);
    transition: var(--transition-fast);
    border-radius: 1px;
  }

  .input-group:focus-within::after {
    width: 100%;
  }

  /* Divisor de seção elegante */
  .section-divider {
    border: none;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--primary-color), transparent);
    margin: 3rem 0;
    opacity: 0.3;
    position: relative;
  }

  .section-divider::before {
    content: '✦';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: white;
    color: var(--primary-color);
    padding: 0 1rem;
    font-size: 1.2rem;
  }

  /* Alerta de erro redesenhado */
  .alert-danger-custom {
    background: linear-gradient(135deg, var(--danger-bg) 0%, #fef7f7 100%);
    color: var(--danger-text);
    border: 2px solid var(--danger-border);
    border-radius: var(--border-radius-sm);
    margin-bottom: 2rem;
    box-shadow: 0 8px 25px -8px rgba(220, 38, 38, 0.2);
    position: relative;
    overflow: hidden;
  }

  .alert-danger-custom::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: var(--danger-text);
  }

  .alert-danger-custom .alert-heading {
    font-weight: 600;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
  }

  .alert-danger-custom ul {
    padding-left: 1.5rem;
    margin-bottom: 0;
  }

  .alert-danger-custom li {
    margin-bottom: 0.5rem;
    font-weight: 500;
  }

  /* Seção de cálculo premium */
  .calculation-section {
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border: 2px solid #e2e8f0;
    border-radius: var(--border-radius-sm);
    padding: 2rem;
    margin: 2rem 0;
    position: relative;
    overflow: hidden;
    transition: var(--transition-fast);
  }

  .calculation-section::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(99, 102, 241, 0.05) 0%, transparent 70%);
    border-radius: 50%;
  }

  .calculation-section:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-soft);
  }

  .calculation-title {
    color: var(--text-color);
    font-weight: 700;
    font-size: 1.25rem;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    position: relative;
    z-index: 2;
  }

  .calculation-title i {
    color: var(--primary-color);
    font-size: 1.5rem;
  }

  .calculation-summary {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
    font-size: 1rem;
    position: relative;
    z-index: 2;
  }

  .calculation-summary li {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 1.5rem;
    margin-bottom: 0.75rem;
    background: rgba(255, 255, 255, 0.8);
    border-radius: var(--border-radius-input);
    border: 1px solid rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(10px);
    transition: var(--transition-fast);
  }

  .calculation-summary li:hover {
    background: rgba(255, 255, 255, 0.95);
    transform: translateX(8px);
  }

  .calculation-summary span {
    color: var(--text-muted);
    font-weight: 500;
  }

  .calculation-summary strong {
    font-weight: 700;
    color: var(--text-color);
    font-size: 1.05rem;
  }

  /* Bloco de valor total premium */
  .total-summary {
    background: var(--success-gradient);
    border: none;
    border-radius: var(--border-radius-sm);
    padding: 2rem;
    text-align: center;
    color: white;
    box-shadow: 0 10px 30px -5px rgba(16, 185, 129, 0.3);
    position: relative;
    overflow: hidden;
    z-index: 2;
  }

  .total-summary::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200px;
    height: 200px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(180deg); }
  }

  .total-label {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    opacity: 0.95;
    position: relative;
    z-index: 3;
  }

  .total-label i {
    font-size: 1.25rem;
  }

  .total-value {
    font-size: 2.5rem;
    font-weight: 800;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
    position: relative;
    z-index: 3;
  }

  /* Botão de envio espetacular */
  .btn-submit {
    background: var(--primary-gradient);
    border: none;
    padding: 1rem 3rem;
    font-size: 1.1rem;
    font-weight: 700;
    color: white;
    border-radius: var(--border-radius-sm);
    transition: var(--transition-fast);
    text-transform: uppercase;
    letter-spacing: 1px;
    box-shadow: 0 8px 25px -5px rgba(99, 102, 241, 0.4);
    position: relative;
    overflow: hidden;
  }

  .btn-submit::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: var(--transition-fast);
  }

  .btn-submit:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 35px -5px rgba(99, 102, 241, 0.5);
  }

  .btn-submit:hover::before {
    left: 100%;
  }

  .btn-submit:active {
    transform: translateY(-2px);
  }

  /* Animações de entrada */
  .form-card {
    animation: slideInUp 0.8s ease-out;
  }

  @keyframes slideInUp {
    from {
      opacity: 0;
      transform: translateY(50px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .mb-4 {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
    transform: translateY(20px); /* Posição inicial para animação */
  }

  .mb-4:nth-child(1) { animation-delay: 0.1s; }
  .mb-4:nth-child(2) { animation-delay: 0.2s; }
  .mb-4:nth-child(3) { animation-delay: 0.3s; }
  .mb-4:nth-child(4) { animation-delay: 0.4s; }
  .mb-4:nth-child(5) { animation-delay: 0.5s; }
  .mb-4:nth-child(6) { animation-delay: 0.6s; }


  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsividade melhorada */
  @media (max-width: 768px) {
    .main-container {
      padding: 0 1rem;
    }

    .card-body, .card-header {
      padding: 2rem 1.5rem;
    }

    .card-header h2 {
      font-size: 1.5rem;
    }

    .form-control {
      height: 50px;
      font-size: 0.95rem;
    }

    .calculation-section {
      padding: 1.5rem;
    }

    .total-value {
      font-size: 2rem;
    }

    .btn-submit {
      padding: 0.875rem 2rem;
      font-size: 1rem;
    }
  }

  @media (max-width: 576px) {
    body {
      padding: 1rem 0;
    }

    .card-header, .card-body {
      padding: 1.5rem 1rem;
    }

    .calculation-summary li {
      padding: 0.75rem 1rem;
      flex-direction: column;
      align-items: flex-start;
      gap: 0.25rem;
    }

    .total-summary {
      padding: 1.5rem;
    }
  }
</style>
</head>
<body>

  <div class="main-container">
    <noscript>
      <div class="alert alert-warning" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        O cálculo automático requer JavaScript habilitado.
      </div>
    </noscript>

    <div class="form-card" data-controller="proposta-form">
      <div class="card-header">
        <h2>
          <i class="bi bi-file-earmark-plus"></i>
          Nova Proposta
        </h2>
      </div>

      <div class="card-body">
        <%# <% if @proposta.errors.any? %>
        <div class="alert alert-danger-custom" role="alert" style="display: none;"> <#% Ocultado para exibição %>
            <h6 class="alert-heading">
              <i class="bi bi-exclamation-triangle-fill me-2"></i>
              Atenção aos seguintes erros:
            </h6>
            <ul>
              <%# <% @proposta.errors.full_messages.each do |msg| %>
                <%# <li><%= msg %></li>
              <%# <% end %>
            </ul>
          </div>
        <%# <% end %>

        <%# <%= form_with(model: @proposta, local: true) do |form| %>
        <form> <%# Usando uma tag form padrão para demonstração %>
          <div class="row">
            <div class="col-md-6 mb-4">
              <%# <%= form.label :revisao, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-arrow-repeat"></i> Revisão
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text"><i class="bi bi-hash"></i></span>
                <%# <%= form.number_field :revisao, class: "form-control", ... %>
                <input type="number" class="form-control" min="1" required placeholder="Ex: 1"
                       data-proposta-form-target="revisao"
                       data-action="input->proposta-form#calculate" value="1">
              </div>
            </div>

            <div class="col-md-6 mb-4">
              <%# <%= form.label :quantidade_horas, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-clock-history"></i> Horas Previstas
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text"><i class="bi bi-clock"></i></span>
                <%# <%= form.number_field :quantidade_horas, class: "form-control", ... %>
                <input type="number" class="form-control" step="0.5" min="0" required placeholder="Ex: 40.5"
                       data-proposta-form-target="horas"
                       data-action="input->proposta-form#calculate">
              </div>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 mb-4">
               <%# <%= form.label :valor_hora, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-currency-dollar"></i> Valor da Hora
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text">R$</span>
                 <%# <%= form.number_field :valor_hora, class: "form-control", ... %>
                <input type="number" class="form-control" step="0.01" min="0" required placeholder="150.00"
                       data-proposta-form-target="valorHora"
                       data-action="input->proposta-form#calculate">
              </div>
            </div>

            <div class="col-md-6 mb-4">
              <%# <%= form.label :despesa_indireta, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-receipt"></i> Despesas Indiretas
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text">R$</span>
                <%# <%= form.number_field :despesa_indireta, class: "form-control", ... %>
                <input type="number" class="form-control" step="0.01" min="0" required placeholder="350.00"
                       data-proposta-form-target="despesas"
                       data-action="input->proposta-form#calculate">
              </div>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 mb-4">
              <%# <%= form.label :margem_lucro_fixa, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-percent"></i> Margem Fixa
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text">%</span>
                <%# <%= form.number_field :margem_lucro_fixa, class: "form-control", ... %>
                <input type="number" class="form-control" step="0.01" min="0" max="100" required placeholder="20.00"
                       data-proposta-form-target="margemFixa"
                       data-action="input->proposta-form#calculate">
              </div>
            </div>

            <div class="col-md-6 mb-4">
              <%# <%= form.label :margem_lucro, class: "form-label" do %>
              <label class="form-label">
                <i class="bi bi-graph-up-arrow"></i> Margem Adicional
              </label>
              <%# <% end %>
              <div class="input-group">
                <span class="input-group-text">%</span>
                <%# <%= form.number_field :margem_lucro, class: "form-control", ... %>
                <input type="number" class="form-control" step="0.01" min="0" max="100" required placeholder="15.00"
                       data-proposta-form-target="margemAdicional"
                       data-action="input->proposta-form#calculate">
              </div>
            </div>
          </div>

          <hr class="section-divider">

          <div class="calculation-section">
            <h5 class="calculation-title">
              <i class="bi bi-calculator-fill"></i>
              Resumo da Proposta
            </h5>

            <ul class="calculation-summary">
              <li>
                <span>Valor Base (Horas × Valor/Hora)</span>
                <strong data-proposta-form-target="valorBaseDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Despesas Indiretas</span>
                <strong data-proposta-form-target="despesasDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Margem Fixa</span>
                <strong data-proposta-form-target="margemFixaDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Margem Adicional</span>
                <strong data-proposta-form-target="margemAdicionalDisplay">R$ 0,00</strong>
              </li>
            </ul>

            <div class="total-summary" aria-live="polite">
              <div class="total-label">
                <i class="bi bi-cash-stack"></i>
                Valor Total da Proposta
              </div>
              <div class="total-value" data-proposta-form-target="valorTotalDisplay">
                R$ 0,00
              </div>
            </div>
            <%# <%= form.hidden_field :valor_total, "data-proposta-form-target" => "valorTotalHidden" %>
            <input type="hidden" data-proposta-form-target="valorTotalHidden">
          </div>

          <div class="d-grid mt-4">
            <%# <%= form.submit "Criar Proposta", class: "btn btn-submit" %>
            <button type="submit" class="btn btn-submit">Criar Proposta</button>
          </div>
        <%# <% end %>
        </form>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>

</body>
</html>
