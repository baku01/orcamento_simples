<!DOCTYPE html>
<html>
<head>
  <title>Nova Proposta</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
    :root {
      --primary-color: #6366f1;
      --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      --success-color: #10b981;
      --danger-bg: #fef2f2;
      --danger-text: #dc2626;
      --body-bg: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
      --card-bg: rgba(255, 255, 255, 0.95);
      --text-color: #1e293b;
      --border-radius: 16px;
      --font-family: 'Inter', sans-serif;
    }
    body {
      background: var(--body-bg);
      font-family: var(--font-family);
      color: var(--text-color);
      font-size: 14px;
      line-height: 1.6;
      padding: 2rem 0;
    }
    .main-container {
      max-width: 950px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }
    .form-card {
      background: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      margin-bottom: 2rem;
    }
    .form-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.15);
    }
    .card-header {
      background: var(--primary-gradient);
      color: white;
      padding: 2rem 2.5rem;
      border-radius: var(--border-radius) var(--border-radius) 0 0;
      position: relative;
    }
    .card-header h2 {
      font-weight: 600;
      font-size: 1.75rem;
      margin: 0;
      display: flex;
      align-items: center;
      gap: 1rem;
      position: relative;
      z-index: 2;
    }
    .card-header h2 i {
      font-size: 2rem;
      opacity: 0.9;
    }
    .card-body {
      padding: 2.5rem;
    }
    .form-label {
      font-weight: 600;
      color: var(--text-color);
      margin-bottom: 0.75rem;
      font-size: 0.875rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .form-label i {
      color: var(--primary-color);
      font-size: 1rem;
    }
    .input-group {
      position: relative;
      margin-bottom: 0.5rem;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .input-group-text {
      background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
      border: 2px solid #e2e8f0;
      border-right: none;
      color: var(--primary-color);
      font-weight: 600;
      font-size: 0.9rem;
      border-radius: 10px 0 0 10px;
      padding: 0 1rem;
      display: flex;
      align-items: center;
      min-width: 60px;
      justify-content: center;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .form-control {
      height: 56px;
      border: 2px solid #e2e8f0;
      border-radius: 0 10px 10px 0;
      font-size: 1rem;
      font-weight: 500;
      padding: 0 1.25rem;
      background: #ffffff;
      color: var(--text-color);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 8px -2px rgba(0, 0, 0, 0.05);
      flex: 1 1 auto;
      width: 1%;
      min-width: 0;
    }
    .form-control:focus {
      border-color: var(--primary-color);
      background: #f8fafc;
      outline: none;
    }
    .section-divider {
      border: none;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--primary-color), transparent);
      margin: 3rem 0;
      opacity: 0.3;
      position: relative;
    }
    .calculation-section {
      background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
      border: 2px solid #e2e8f0;
      border-radius: 12px;
      padding: 2rem;
      margin: 2rem 0;
      position: relative;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .calculation-title {
      color: var(--text-color);
      font-weight: 700;
      font-size: 1.25rem;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
      position: relative;
      z-index: 2;
    }
    .calculation-title i {
      color: var(--primary-color);
      font-size: 1.5rem;
    }
    .calculation-summary {
      list-style: none;
      padding: 0;
      margin: 0 0 2rem 0;
      font-size: 1rem;
      position: relative;
      z-index: 2;
    }
    .calculation-summary li {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 1.5rem;
      margin-bottom: 0.75rem;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, 0.5);
      backdrop-filter: blur(10px);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    .calculation-summary li:hover {
      background: rgba(255, 255, 255, 0.95);
      transform: translateX(8px);
    }
    .calculation-summary span {
      color: #64748b;
      font-weight: 500;
    }
    .calculation-summary strong {
      font-weight: 700;
      color: var(--text-color);
      font-size: 1.05rem;
    }
    .total-summary {
      background: linear-gradient(135deg, #10b981 0%, #34d399 100%);
      border: none;
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      color: white;
      font-size: 1.5rem;
      font-weight: 700;
      box-shadow: 0 10px 30px -5px rgba(16, 185, 129, 0.3);
      position: relative;
      overflow: hidden;
      z-index: 2;
      margin-top: 1rem;
    }
    .total-label {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 0.75rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      opacity: 0.95;
      position: relative;
      z-index: 3;
    }
    .total-value {
      font-size: 2.5rem;
      font-weight: 800;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
      position: relative;
      z-index: 3;
    }
    .btn-submit {
      background: var(--primary-gradient);
      border: none;
      padding: 1rem 3rem;
      font-size: 1.1rem;
      font-weight: 700;
      color: white;
      border-radius: 12px;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 8px 25px -5px rgba(99, 102, 241, 0.4);
      position: relative;
      overflow: hidden;
    }
    .btn-submit:hover {
      transform: translateY(-4px);
      box-shadow: 0 15px 35px -5px rgba(99, 102, 241, 0.5);
    }
    @media (max-width: 768px) {
      .main-container { padding: 0 1rem; }
      .card-body, .card-header { padding: 2rem 1.5rem; }
      .form-control { height: 50px; font-size: 0.95rem; }
      .calculation-section { padding: 1.5rem; }
      .total-summary { padding: 1.5rem; font-size: 1.2rem; }
      .total-value { font-size: 2rem; }
      .btn-submit { padding: 0.875rem 2rem; font-size: 1rem; }
    }
    @media (max-width: 576px) {
      body { padding: 1rem 0; }
      .card-header, .card-body { padding: 1.5rem 1rem; }
      .calculation-summary li { padding: 0.75rem 1rem; flex-direction: column; align-items: flex-start; gap: 0.25rem; }
      .total-summary { padding: 1.5rem; }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <noscript>
      <div class="alert alert-warning" role="alert">
        <i class="bi bi-exclamation-triangle-fill me-2"></i>
        O cálculo automático requer JavaScript habilitado.
      </div>
    </noscript>
    <div class="form-card" data-controller="proposta-form">
      <div class="card-header">
        <h2>
          <i class="bi bi-file-earmark-plus"></i>
          Nova Proposta
        </h2>
      </div>
      <div class="card-body">
        <% if @proposta.errors.any? %>
          <div class="alert alert-danger-custom" role="alert">
            <h6 class="alert-heading">
              <i class="bi bi-exclamation-triangle-fill me-2"></i>
              Atenção aos seguintes erros:
            </h6>
            <ul>
              <% @proposta.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        <%= form_with(model: @proposta, local: true, html: {autocomplete: "off"}) do |form| %>
          <div class="row">
            <div class="col-md-6 mb-4">
              <%= form.label :revisao, class: "form-label" do %>
                <i class="bi bi-arrow-repeat"></i> Revisão
              <% end %>
              <div class="input-group">
                <span class="input-group-text"><i class="bi bi-hash"></i></span>
                <%= form.number_field :revisao, class: "form-control", min: 1, required: true, placeholder: "Ex: 1", data: { proposta_form_target: "revisao", action: "input->proposta-form#calculate" }, value: @proposta.revisao || 1 %>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <%= form.label :quantidade_horas, class: "form-label" do %>
                <i class="bi bi-clock-history"></i> Horas Previstas
              <% end %>
              <div class="input-group">
                <span class="input-group-text"><i class="bi bi-clock"></i></span>
                <%= form.number_field :quantidade_horas, class: "form-control", step: 0.5, min: 0, required: true, placeholder: "Ex: 40.5", data: { proposta_form_target: "horas", action: "input->proposta-form#calculate" } %>
              </div>
            </div>
          </div>
          <div class="row">
            <div class="col-md-6 mb-4">
              <%= form.label :valor_hora, class: "form-label" do %>
                <i class="bi bi-currency-dollar"></i> Valor da Hora
              <% end %>
              <div class="input-group">
                <span class="input-group-text">R$</span>
                <%= form.number_field :valor_hora, class: "form-control", step: 0.01, min: 0, required: true, placeholder: "150.00", data: { proposta_form_target: "valorHora", action: "input->proposta-form#calculate" } %>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <%= form.label :despesa_indireta, class: "form-label" do %>
                <i class="bi bi-receipt"></i> Despesas Indiretas
              <% end %>
              <div class="input-group">
                <span class="input-group-text">R$</span>
                <%= form.number_field :despesa_indireta, class: "form-control", step: 0.01, min: 0, required: true, placeholder: "350.00", data: { proposta_form_target: "despesas", action: "input->proposta-form#calculate" } %>
              </div>
            </div>
          </div>
          <div class="row">
            <div class="col-md-6 mb-4">
              <%= form.label :margem_lucro_fixa, class: "form-label" do %>
                <i class="bi bi-percent"></i> Margem Fixa
              <% end %>
              <div class="input-group">
                <span class="input-group-text">%</span>
                <%= form.number_field :margem_lucro_fixa, class: "form-control", step: 0.01, min: 0, max: 100, required: true, placeholder: "20.00", data: { proposta_form_target: "margemFixa", action: "input->proposta-form#calculate" } %>
              </div>
            </div>
            <div class="col-md-6 mb-4">
              <%= form.label :margem_lucro, class: "form-label" do %>
                <i class="bi bi-graph-up-arrow"></i> Margem Adicional
              <% end %>
              <div class="input-group">
                <span class="input-group-text">%</span>
                <%= form.number_field :margem_lucro, class: "form-control", step: 0.01, min: 0, max: 100, required: true, placeholder: "15.00", data: { proposta_form_target: "margemAdicional", action: "input->proposta-form#calculate" } %>
              </div>
            </div>
          </div>
          <hr class="section-divider">
          <div class="calculation-section">
            <h5 class="calculation-title">
              <i class="bi bi-calculator-fill"></i>
              Resumo da Proposta
            </h5>
            <ul class="calculation-summary">
              <li>
                <span>Valor Base (Horas × Valor/Hora)</span>
                <strong data-proposta-form-target="valorBaseDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Despesas Indiretas</span>
                <strong data-proposta-form-target="despesasDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Margem Fixa</span>
                <strong data-proposta-form-target="margemFixaDisplay">R$ 0,00</strong>
              </li>
              <li>
                <span>+ Margem Adicional</span>
                <strong data-proposta-form-target="margemAdicionalDisplay">R$ 0,00</strong>
              </li>
            </ul>
            <div class="total-summary" aria-live="polite">
              <div class="total-label">
                <i class="bi bi-cash-stack"></i>
                Valor Total da Proposta
              </div>
              <div class="total-value" data-proposta-form-target="valorTotalDisplay">
                R$ 0,00
              </div>
            </div>
            <%= form.hidden_field :valor_total, data: { proposta_form_target: "valorTotalHidden" } %>
          </div>
          <div class="d-grid mt-4">
            <%= form.submit "Criar Proposta", class: "btn btn-submit" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
</body>
</html>
