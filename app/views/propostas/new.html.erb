<!DOCTYPE html>
<html>
<head>
  <title>Nova Proposta | Versão Dinâmica</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

    :root {
      --primary-color: #6366f1;
      --primary-rgb: 99, 102, 241;
      --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      --success-color: #10b981;
      --success-rgb: 16, 185, 129;
      --light-bg: #f8fafc;
      --card-bg: #ffffff;
      --text-color: #1e293b;
      --muted-text-color: #64748b;
      --border-color: #e2e8f0;
      --border-radius: 1rem;
      --font-family: 'Inter', sans-serif;
    }

    body {
      background-color: var(--light-bg);
      font-family: var(--font-family);
      color: var(--text-color);
    }

    .form-section {
      background-color: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05), 0 2px 4px -2px rgba(0,0,0,0.05);
      border: 1px solid var(--border-color);
      padding: 2.5rem;
    }

    .summary-section {
      border-radius: var(--border-radius);
      padding: 2rem;
      background-color: var(--card-bg);
      border: 1px solid var(--border-color);
    }

    .form-label {
      font-weight: 600;
      font-size: 0.875rem;
      margin-bottom: 0.5rem;
    }

    .form-control, .form-select {
        height: 50px;
        border: 1px solid var(--border-color);
        font-size: 1rem;
        background-color: #fff;
    }
    .form-control:disabled {
        background-color: var(--light-bg);
        cursor: not-allowed;
    }
    .form-control:focus, .form-select:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(var(--primary-rgb), 0.2);
        outline: none;
    }

    .section-title {
        font-weight: 700;
        font-size: 1.25rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
    }
    .section-title i { color: var(--primary-color); }

    /* Toggle Switch */
    .form-switch { padding-left: 3.5em; }
    .form-check-input {
      width: 2.5em;
      height: 1.5em;
      cursor: pointer;
      background-color: var(--border-color);
      border-color: var(--border-color);
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='-4 -4 8 8'%3e%3ccircle r='3' fill='%23fff'/%3e%3c/svg%3e");
    }
    .form-check-input:checked {
      background-color: var(--success-color);
      border-color: var(--success-color);
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='-4 -4 8 8'%3e%3ccircle r='3' fill='%23fff'/%3e%3c/svg%3e");
    }

    /* Tabela de Funções Interativa */
    .funcoes-table tbody tr {
      transition: background-color 0.3s ease;
    }
    .funcoes-table tbody tr.is-active {
      background-color: rgba(var(--success-rgb), 0.07);
    }
    .funcoes-table td {
        vertical-align: middle;
    }
    .subtotal-cell {
        font-weight: 600;
        font-size: 0.95rem;
        color: var(--muted-text-color);
        text-align: right;
    }

    /* Resumo da Proposta */
    .summary-list li {
      display: flex;
      justify-content: space-between;
      padding: 1rem 0;
      border-bottom: 1px solid var(--border-color);
    }
    .summary-list li:last-child { border-bottom: none; }
    .summary-list span { color: var(--muted-text-color); }
    .summary-list strong { font-weight: 600; }

    .total-summary {
      background: var(--primary-gradient);
      border-radius: 0.75rem;
      padding: 1.5rem 2rem;
      margin-top: 1.5rem;
      color: white;
      text-align: left;
    }
    .total-label { font-weight: 500; opacity: 0.9; margin-bottom: 0.25rem; }
    .total-value { font-size: 2.25rem; font-weight: 800; }

    .btn-submit {
      background: var(--primary-gradient);
      border: none;
      padding: 1rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      color: white;
      border-radius: 0.5rem;
      width: 100%;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      box-shadow: 0 4px 15px -5px rgba(var(--primary-rgb), 0.4);
    }
    .btn-submit:hover {
      transform: translateY(-3px);
      box-shadow: 0 7px 25px -5px rgba(var(--primary-rgb), 0.5);
    }

  </style>
</head>
<body>
  <div class="container py-5">
    <%= form_with(model: @proposta, local: true, html: {autocomplete: "off", 'data-controller': 'proposta-form'}) do |form| %>
      <div class="row g-5">

        <div class="col-lg-7">
          <div class="form-section">
            <h1 class="h2 mb-2 fw-bold">Nova Proposta</h1>
            <p class="text-muted mb-5">Preencha os dados abaixo para gerar o cálculo da proposta.</p>

            <div class="row">
              <div class="col-md-6 mb-4">
                <%= form.label :revisao, class: "form-label" %>
                <div class="input-group">
                  <span class="input-group-text"><i class="bi bi-arrow-repeat"></i></span>
                  <%= form.number_field :revisao, class: "form-control", min: 1, required: true, placeholder: "Revisão", 'data-action': 'input->proposta-form#update' %>
                </div>
              </div>
              <div class="col-md-6 mb-4">
                <%= form.label :margem_lucro, "Margem Adicional (%)", class: "form-label" %>
                <div class="input-group">
                  <span class="input-group-text"><i class="bi bi-graph-up-arrow"></i></span>
                  <%= form.number_field :margem_lucro, class: "form-control", step: 0.01, min: 0, required: true, placeholder: "15.00", 'data-proposta-form-target': 'margemLucro', 'data-action': 'input->proposta-form#update' %>
                </div>
              </div>
            </div>

            <hr class="my-4">

            <h5 class="section-title"><i class="bi bi-list-check"></i> Funções da Proposta</h5>
            <div class="table-responsive">
              <table class="table funcoes-table">
                <tbody>
                  <% @funcoes.each_with_index do |funcao, idx| %>
                    <tr data-proposta-form-target="funcaoRow">
                      <td class="ps-0">
                        <div class="form-check form-switch">
                          <%= check_box_tag "proposta[proposta_funcoes_attributes][#{idx}][funcao_id]", funcao.id, false,
                              id: "funcao_#{funcao.id}",
                              class: "form-check-input",
                              'data-action': 'change->proposta-form#toggleRow' %>
                        </div>
                      </td>
                      <td><label for="funcao_<%= funcao.id %>" class="fw-bold"><%= funcao.nome %></label></td>
                      <td style="min-width: 130px;">
                        <%= number_field_tag "proposta[proposta_funcoes_attributes][#{idx}][quantidade_horas]", nil,
                            step: 0.5, min: 0, class: "form-control form-control-sm", placeholder: "Horas",
                            disabled: true, 'data-action': 'input->proposta-form#update' %>
                      </td>
                      <td style="min-width: 140px;">
                        <%= number_field_tag "proposta[proposta_funcoes_attributes][#{idx}][valor_hora]", funcao.valor_base,
                            step: 0.01, min: 0, class: "form-control form-control-sm", placeholder: "Valor/h",
                            disabled: true, 'data-action': 'input->proposta-form#update' %>
                      </td>
                      <td class="subtotal-cell">
                          —
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>

            <div class="d-grid mt-5">
              <%= form.submit "Criar Proposta", class: "btn btn-submit" %>
            </div>

          </div>
        </div>

        <div class="col-lg-5">
          <div class="sticky-top" style="top: 2rem;">
            <div class="summary-section">
              <h5 class="section-title"><i class="bi bi-calculator"></i> Resumo da Proposta</h5>
              <ul class="list-unstyled summary-list">
                <li>
                  <span>Valor Base</span>
                  <strong data-proposta-form-target="valorBaseDisplay">R$ 0,00</strong>
                </li>
                <li>
                  <span>Margem Adicional</span>
                  <strong data-proposta-form-target="margemAdicionalDisplay">R$ 0,00</strong>
                </li>
              </ul>
              <div class="total-summary" aria-live="polite">
                <div class="total-label">Valor Total</div>
                <div class="total-value" data-proposta-form-target="valorTotalDisplay">R$ 0,00</div>
              </div>
              <%= form.hidden_field :valor_total, data: { proposta_form_target: "valorTotalHidden" } %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
</body>
</html>
